// Empty.swift - the shared library's own translation unit
//
// A CMake SHARED target needs at least one source of its own to establish the link language.
// Everything the library actually contains arrives from the archives linked into it, whole, by
// the rules in CMakeLists.txt.
